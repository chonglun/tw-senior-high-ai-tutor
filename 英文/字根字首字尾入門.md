---
科目: 英文
主題: 字根字首入門
課綱對應: 英文-核心字彙與構詞
難度: ★★
考試頻率: 高
標籤:
  - 字彙
  - 字根
  -  字首
  -  字尾
教學風格Agent: 字根拆解記憶 + 情境圖解文法 + 實戰口訣題型教練
檢查狀態: 已檢查
信心度: 高
更新日期: 2026-06-28
---

# 字根字首入門
```mermaid
graph LR
    %% 核心架構：左到右的單字生成線
    
    subgraph Pipeline ["🧩 英文單字超級組裝流水線"]
        Prefix["<b>【字首 Prefix】</b><br>🎬 角色：方向盤<br>🎯 功能：決定方向與正負情緒"] 
        style Prefix fill:#e1f5fe,stroke:#0288d1,stroke-width:2px
        
        Root(("<b>【字根 Root】</b><br>❤️ 角色：核心引擎<br>🎯 功能：決定底層核心畫面"))
        style Root fill:#ff9999,stroke:#c62828,stroke-width:3px
        
        Suffix["<b>【字尾 Suffix】</b><br>🛠️ 角色：變速箱<br>🎯 功能：不改詞義只決定詞性"]
        style Suffix fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
    end

    %% 組裝流程（修正：將 1. 2. 3. 移除或改用雙引號，徹底解決 Unsupported markdown list 錯誤）
    Prefix ==> |"撥動方向"| Root
    Root ==> |"輸出馬力"| Suffix
    Suffix ==> |"最終出廠"| Output[("📦 最終單字產出")]
    style Output fill:#fff9c4,stroke:#fbc02d,stroke-width:2px

```
## 💡 為什麼要學？（Start with Why）
> 高中要會 4500+ 單字，硬背到懷疑人生？其實英文單字像樂高，多由「字首＋字根＋字尾」組成。學會拆解，看到沒背過的生字也能猜意思——這不只幫你拿下學測詞彙題，更是讀原文、看影集、查資料能用一輩子的「解碼力」。

## 📌 一句話總結
> 單字不是死背，而是把「字首＋字根＋字尾」三段樂高拼回意思，一次記住一整串同源字。

## 🎯 核心概念
> 先建「拆解工具箱」，再看任何字根清單——沒有工具箱，清單只是另一張要硬背的生字表。

**拆解公式**：`單字 ＝ 字首（方向/正負）＋ 字根（核心動作）＋ 字尾（詞性/狀態）`

- **字首（Prefix）定方向**：改變方向、正負、有無。如 ex-（向外）、in-（向內）、re-（重複/向後）。
- **字根（Root）定核心**：決定這個字最底層的「畫面／動作」，是一群同源字共用的中心。
- **字尾（Suffix）定詞性**：決定詞性與狀態。如 -tion（名詞）、-ate（動詞）、-ive（形容詞）。
- 同一字根可串出一群同源字，記住一個「核心畫面」就帶出五到八個字。
- 拆解的價值在「看到生字也能猜方向」，而非每個字都查中譯。

## 🗺 圖解
> 一個字根帶一串：以 port（搬運）為中心的同源字家族。
```mermaid
graph LR
  root("字根 port = 搬運") --> transport["transport 運輸"]
  transport --> trans["trans- 橫越、跨越"]
  
  root --> export["export 出口"]
  export --> ex["ex- 向外、出"]
  
  root --> import["import 進口"]
  import --> im["im- 向內、入"]
  
  root --> portable["portable 可攜帶的"]
  portable --> able["-able 可...的"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```
## 🌏 生活連結（記憶錨點）
> - 把單字想成樂高：字首是底座方向、字根是主積木、字尾是裝飾帽。
> - transport：trans-（橫越）＋ port（搬運）→ 把東西搬過去 ＝ 運輸；同 port 串出 export、import、portable。
> ⚠️ 比喻破功處：不是每個字都能乾淨拆三段。有些字根會拼寫變形（spect/spec/spic），有些字首只是「拼湊好唸」沒實義（understand 的 under-），硬拆會誤導——以「常見高頻字根」為主、查證為輔。

## 🧠 記憶法 / 口訣
- 拆字三步：「**先切三段 → 各給意思 → 組回原意**」。
- 字首方向口訣：「**re 回、un 否、ex 出、in 進、trans 越過、pre 先**」。
- 字尾詞性口訣：「**-tion 名、-ate 動、-ive 形、-able 可、-ize 化**」。
- 字根「畫面記憶法」：每個字根先記一個動作畫面（port＝搬東西、spect＝盯著看、ject＝丟出去），畫面對了，整串同源字就跟著浮現（清單見〈考試重點〉）。

## ⭐ 考試重點
- [ ] **能力導向（真正要練的）**：學測不單獨考「字根清單」，考的是「看到生字能拆解、猜對方向」的能力。先練熟拆解工具箱，再用下面的核心字根清單養手感——目標是**反射，不是背數量**。
- [ ] **核心字根清單（起手式，非權威定數）**：坊間對「高頻字根共幾組」沒有單一權威來源，別糾結數量。先吃透這組最常出現的字根，再依讀到的文章自行擴充：

| 字根           | 核心畫面     | 同源字                                  |
| ------------ | -------- | ------------------------------------ |
| port         | 搬運       | transport, export, import, portable  |
| spect/spec   | 看（注視）    | inspect, respect, spectator, suspect |
| dict         | 說        | predict, dictionary, contradict      |
| ject         | 丟        | reject, project, inject              |
| ven/vent     | 來        | prevent, invent, event               |
| scrib/script | 寫        | describe, subscribe, manuscript      |
| duc/duct     | 引導       | conduct, produce, introduce          |
| tract        | 拉        | attract, extract, contract           |
| mit/miss     | 送出       | submit, transmit, dismiss            |
| pos/pon      | 放置       | compose, position, postpone          |
| vid/vis      | 看（視覺/影像） | video, visible, evidence             |
| struct       | 建造       | construct, structure, instruct       |
|              |          |                                      |
```mermaid
graph LR
  root("spect/spec = 看（注視）") --> inspect["inspect 檢查"]
  inspect --> in["in- 向內、進入"]
  root --> respect["respect 尊敬"]
  respect --> re["re- 再、重複、回"]
  root --> spectator["spectator 觀眾"]
  spectator --> ator["-ator 做...的人 (名詞字尾)"]
  root --> suspect["suspect 懷疑"]
  suspect --> sub["sub- 在...之下 (在此變體為 sus-)"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```
```mermaid
graph LR
  root("dict = 說") --> predict["predict 預測"]
  predict --> pre["pre- 提前、預先"]
  root --> dictionary["dictionary 字典"]
  dictionary --> ionary["-ionary 與...有關的人事物/場所 (名詞字尾)"]
  root --> contradict["contradict 反駁/矛盾"]
  contradict --> contra["contra- 反對、相反"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```

```mermaid
graph LR
  root("ject = 丟") --> reject["reject 拒絕"]
  reject --> re["re- 往回、反向"]
  root --> project["project 計畫/投射"]
  project --> pro["pro- 向前"]
  root --> inject["inject 注射"]
  inject --> in["in- 進入、向內"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```


```mermaid
graph LR
  root("ven/vent = 來") --> prevent["prevent 預防/阻止"]
  prevent --> pre["pre- 事先 (來到前面擋住)"]
  root --> invent["invent 發明"]
  invent --> in["in- 進入、到來 (來到心中、偶然發現)"]
  root --> event["event 事件"]
  event --> e["e- 向外、出 (ex-的變體，走出來的結果)"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```
```mermaid
graph LR
  root("scrib/script = 寫") --> describe["describe 描述"]
  describe --> de["de- 向下、寫下"]
  root --> subscribe["subscribe 訂閱"]
  subscribe --> sub["sub- 在...之下 (在文件下方簽名)"]
  root --> manuscript["manuscript 手稿"]
  manuscript --> manu["manu- 手 (用手寫的本子)"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```
```mermaid
graph LR
  root("duc/duct = 引導") --> conduct["conduct 引導/實施"]
  conduct --> con["con- 共同、一起"]
  root --> produce["produce 生產"]
  produce --> pro["pro- 向前"]
  root --> introduce["introduce 介紹"]
  introduce --> intro_def["intro- 向內、內部"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```
```mermaid
graph LR
  root("tract = 拉") --> attract["attract 吸引"]
  attract --> ad["ad- 朝向、往 (在此變體為 at-)"]
  root --> extract["extract 萃取/提取"]
  extract --> ex["ex- 向外、出"]
  root --> contract["contract 合約/收縮"]
  contract --> con["con- 共同、拉到一起"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```
```mermaid
graph LR
  root("mit/miss = 送出") --> submit["submit 提交/服從"]
  submit --> sub["sub- 在...之下 (送交到下位、屈服)"]
  root --> transmit["transmit 傳送"]
  transmit --> trans["trans- 橫越、跨越"]
  root --> dismiss["dismiss 解散/解雇"]
  dismiss --> dis["dis- 分開、散開"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```
```mermaid
graph LR
  root("pos/pon = 放置") --> compose["compose 組成/作曲"]
  compose --> con["con- 一起 (放在一起)"]
  root --> position["position 位置"]
  position --> ition["-ition 動作、狀態、結果 (名詞字尾)"]
  root --> postpone["postpone 延期"]
  postpone --> post["post- 在...之後"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```
```mermaid
graph LR
  root("vid/vis = 看（視覺/影像）") --> video["video 影片"]
  video --> eo["-eo 相關媒介/事物 (名詞字尾)"]
  root --> visible["visible 可見的"]
  visible --> ible["-ible 可...的 (形容詞字尾)"]
  root --> evidence["evidence 證據"]
  evidence --> e["e- 向外、出 (ex-的變體，看出來的跡象)"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```
```mermaid
graph LR
  root("struct = 建造") --> construct["construct 建造"]
  construct --> con["con- 共同、一起 (建造在一起)"]
  root --> structure["structure 結構"]
  structure --> ure["-ure 狀態、行為、結果 (名詞字尾)"]
  root --> instruct["instruct 指導"]
  instruct --> in["in- 向內 (在內部建立知識)"]
  
  %% 調整一下視覺顏色，讓它更像心智圖
  style root fill:#f9f,stroke:#333,stroke-width:2px
```

- [ ] **常考題型**：詞彙題直接考同源字辨義；綜合測驗、文意選填靠拆字猜生字。
- [ ] **注意**：同源字「形近義異」是常見誘答（adapt/adopt、affect/effect），須連詞性一起記。

## ⚠️ 易錯點 / 陷阱
- 把所有字硬拆：例外字（understand、example）拆了反而記錯。
- 只記字首、忽略字尾詞性 → 填空詞性錯（名詞填到動詞位）。
- 字首遇字根會「同化變形」：in- 在 p 前變 im-（import）、l 前變 il-（illegal），拼字易錯。
- 拆字只給「語意方向」，精準字義仍要看語境，別只憑拆字硬填。

## 🔗 跨科連結
- [[字根（Root）定核心]]
- [[綜合測驗與文意選填]]
- [[閱讀測驗策略]]
- [[學名與拉丁字根]]

## 📝 一分鐘自我檢測
> 先遮答案再想。
1. Q：拆解 export 並說出意義。　A：ex-（向外）＋port（搬運）→ 出口/輸出。
2. Q：respect 與 inspect 共用哪個字根？各自意義？　A：spect（看）；respect＝反覆看＝尊敬、inspect＝往內看＝檢查。
3. Q：-tion、-able、-ize 各是什麼詞性？　A：名詞、形容詞、動詞。

---
> 📋 參考：大考中心《高中英文參考詞彙表》 https://www.ceec.edu.tw/xmdoc?xsmsid=0K213553204833715309


